name: Build and Deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.html'
      - 'assets/**'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm install
      
      - name: Build
        run: npm run build
      
      - name: Move dist contents to root
        run: |
          # Backup source index.html
          mv index.source.html index.html.backup 2>/dev/null || true
          # Remove old built files
          rm -f index.html vite.svg
          rm -rf assets
          # Copy new built files
          cp -r dist/* .
          # Restore source index for development
          mv index.html.backup index.source.html 2>/dev/null || true
      
      - name: Commit and push
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Deploy: Update built files [skip ci]"
          git push
